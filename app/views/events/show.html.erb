Where is my view?

<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <link rel="stylesheet" type="text/css" href="index.scss">
  <title>show</title>
</head>
<body>
   <h1> Day : <%= @day = Date.parse(event_params).strftime("%B %e, %Y")%> </h1>
  <h2> <%= link_to "Home", calendar_path, class: "btn btn-default" %> </h2>
  <h2> <%= link_to "Add Event", new_event_path, class: "btn btn-default" %> </h2>
  <table>
    <thead>
      <tr>
        <th>Event</th>
        <th>Time</th>
        <th>Location</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= @events.date.today %>
          <td><%= @events.each do |event| %>
            <ul>  <%= event.name %> </ul>
            <ul>  <%= event.date_and_time %> </ul>
            <ul>  <%= event.location %> </ul>
            <%end %>
          </td>
      </tr>
    </tbody>
</html>
