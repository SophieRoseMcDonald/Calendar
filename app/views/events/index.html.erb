<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <link rel="stylesheet" type="text/css" href="index.scss">
  <title></title>
</head>
<body>
  <h1> Day : <%= @day.strftime("%A %d %b %y")-%> </h1>
  <h2> <%= link_to "Home", calendar_path, class: "btn btn-default" %> </h2>
  <h2> <%= link_to "Add Event", new_event_path, class: "btn btn-default" %> </h2>
  <table>
    <thead>
      <tr>
        <th>Event</th>
        <th>Time</th>
        <th>Location</th>
      </tr>
      <tr>
        <td><% @events.each do |event| %>
          <ul>  <%= event.name %></ul>
          <% end %></td>
        <td> <% @events.each do |event| %>
          <ul>  <%= event.date_and_time %> </ul>
          <%end %>
        <td> <% @events.each do |event| %>
          <ul>  <%= event.location %> </ul>
          <%end %>
        </tr>
    </thead>
<tbody>

          </html>
